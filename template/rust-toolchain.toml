[toolchain]
#%if is_riscv
#%if option("stack-smashing-protection")
#+channel    = "nightly"
#%else
channel    = "{{ rust_toolchain }}"
#%endif
components = ["rust-src"]
targets = ["{{ rust_target }}"]
#%else if is_xtensa
#+channel = "{{ rust_toolchain }}"
#%endif
